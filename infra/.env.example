# =============================================================================
# TERRAFORM - Variables de Entorno para Infraestructura
# =============================================================================

# Región de AWS donde se desplegarán todos los recursos
AWS_REGION=us-east-1

# Prefijo para nombrar todos los recursos (ej: inferno-dev, inferno-prod)
# Se usará como: {PROJECT_NAME}-{recurso}
PROJECT_NAME=inferno-bank

# Entorno de despliegue (dev, staging, prod)
# Afecta el tagging y nombrado de recursos
ENVIRONMENT=dev

# Nombre del bucket S3 para el backend remoto de Terraform
# Este bucket almacena el archivo terraform.tfstate
# IMPORTANTE: Debe crearse manualmente antes de ejecutar terraform init
TERRAFORM_STATE_BUCKET=inferno-terraform-state

# Clave (path) dentro del bucket donde se guardará el state file
TERRAFORM_STATE_KEY=inferno-bank/terraform.tfstate

# Email verificado en AWS SES para enviar notificaciones
# DEBE estar verificado en AWS SES antes del despliegue
SES_FROM_EMAIL=noreply@infernobank.com

# Clave secreta para firmar tokens JWT
# RECOMENDACIÓN: Usar al menos 32 caracteres aleatorios
# Ejemplo: openssl rand -base64 32
JWT_SECRET=tu-clave-jwt-super-secreta-de-al-menos-32-caracteres

# Salt rounds para bcrypt (número de iteraciones)
# Valores típicos: 10-12 (mayor = más seguro pero más lento)
PASSWORD_SALT_ROUNDS=10

# Tiempo de expiración de tokens JWT (formato: "24h", "7d", etc.)
JWT_EXPIRATION=24h

# Balance inicial para tarjetas de débito nuevas (en unidad monetaria)
INITIAL_DEBIT_BALANCE=0

# Límite de crédito inicial para tarjetas aprobadas (en unidad monetaria)
INITIAL_CREDIT_LIMIT=5000

# Porcentaje de cashback en transacciones de ahorro (0.2 = 20%)
SAVINGS_CASHBACK_PERCENTAGE=0.2

# Transacciones mínimas de débito requeridas para activar tarjeta de crédito
MIN_TRANSACTIONS_FOR_CREDIT=10

# Tiempo de expiración para URLs firmadas de S3 (en segundos)
# 3600 segundos = 1 hora
PRESIGNED_URL_EXPIRATION=3600

# ARN del rol de ejecución de Lambda (si ya existe, opcional)
# Si se deja vacío, Terraform creará los roles automáticamente
# LAMBDA_EXECUTION_ROLE_ARN=arn:aws:iam::123456789:role/lambda-execution-role

# Tags adicionales para todos los recursos (formato JSON)
# ADDITIONAL_TAGS='{"CostCenter":"Engineering","Owner":"DevOps"}'
