# =============================================================================
# NOTIFICATION SERVICE - Variables de Entorno
# =============================================================================

# Región de AWS donde se desplegarán los recursos
AWS_REGION=us-east-1

# Nombre de la tabla DynamoDB para usuarios
# Se consulta para obtener el email del usuario destinatario
USERS_TABLE_NAME=inferno-users

# Nombre de la tabla DynamoDB para notificaciones
# Registra el historial de todas las notificaciones enviadas
NOTIFICATIONS_TABLE_NAME=inferno-notifications

# Nombre del bucket S3 que contiene las plantillas HTML de emails
# Las plantillas se buscan en: templates/{templateType}.html
# Tipos soportados: welcome, card-approved, card-rejected, credit-activated,
# purchase, savings, payment, low-balance, report-ready
TEMPLATES_BUCKET_NAME=inferno-templates-bucket

# Email verificado en AWS SES que aparecerá como remitente
# IMPORTANTE: Este email debe estar verificado en AWS SES antes de usar
FROM_EMAIL=noreply@infernobank.com

# Nombre que aparecerá como remitente en los emails
FROM_NAME=Inferno Bank

# Asunto predeterminado si no se especifica en el mensaje
DEFAULT_SUBJECT=Notificación de Inferno Bank

# Charset para los emails (generalmente utf-8 para soporte de caracteres especiales)
EMAIL_CHARSET=UTF-8
