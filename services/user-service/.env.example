# =============================================================================
# USER SERVICE - Variables de Entorno
# =============================================================================

# Región de AWS donde se desplegarán los recursos
AWS_REGION=us-east-1

# Nombre de la tabla DynamoDB para usuarios
# Almacena: userId, email, password (hash), fullName, createdAt, avatar (URL S3)
USERS_TABLE_NAME=inferno-users

# Nombre de la tabla DynamoDB para notificaciones
# Registra todas las notificaciones enviadas a los usuarios
NOTIFICATIONS_TABLE_NAME=inferno-notifications

# URL de la cola SQS para enviar notificaciones
# El notification service consume mensajes de esta cola
NOTIFICATIONS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789/inferno-notifications-queue

# Nombre del bucket S3 para almacenar avatares de usuarios
# Los avatares se almacenan con la ruta: avatars/{userId}.{extension}
AVATARS_BUCKET_NAME=inferno-avatars-bucket

# Nombre del secret en AWS Secrets Manager que contiene la clave JWT
# Formato del secret: {"jwtSecret": "tu-clave-secreta-aqui"}
JWT_SECRET_NAME=inferno-jwt-secret

# Nombre del secret en AWS Secrets Manager que contiene el salt para bcrypt
# Formato del secret: {"passwordSalt": "10"}
PASSWORD_SALT_SECRET_NAME=inferno-password-salt

# Tiempo de expiración del token JWT (ej: "24h", "7d", "30m")
JWT_EXPIRATION=24h
